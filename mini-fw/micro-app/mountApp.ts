import { App, OptionalLifeCycle } from "./mainApp";

export function mountApp(apps: App[], lifecycle: OptionalLifeCycle) {
    createApp(apps, lifecycle);
}

export function createApp(apps: App[], lifecycle: OptionalLifeCycle) {

}